// Declaration

// Define
function SayHI (name){
    console.log (name);
    return name;
}
// Input
const username = prompt("Enter your name: ");

// Call or Invoke
const callingthename = SayHI (username);

// Console
console.log (callingthename);


//Expression
const sayHi2 = function (name2) {
    return name2;
}

const printname = sayHi2 ("Karen");
console.log (printname);